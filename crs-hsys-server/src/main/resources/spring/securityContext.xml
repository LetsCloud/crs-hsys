<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xsi:schemaLocation="
        http://www.springframework.org/schema/security 
        http://www.springframework.org/schema/security/spring-security.xsd
        http://www.springframework.org/schema/beans 
        http://www.springframework.org/schema/beans/spring-beans.xsd">

	<beans:bean id="appUserDetailsService"
		class="io.crs.hsys.server.security.AppUserDetailsService">
		<beans:constructor-arg ref="appUserService" />
		<beans:constructor-arg ref="loginAttemptService" />
		<beans:constructor-arg ref="modelMapper" />
	</beans:bean>

	<beans:bean id="apiAuthenticationProvider"
		class="org.springframework.security.authentication.dao.DaoAuthenticationProvider">
		<beans:property name="userDetailsService"
			ref="appUserDetailsService" />
	</beans:bean>

	<authentication-manager
		id="apiAuthenticationManager">
		<authentication-provider
			ref="apiAuthenticationProvider" />
	</authentication-manager>

	<!-- =================== -->
	<!-- API SECURITY CONFIG -->
	<!-- =================== -->
	<http create-session="stateless" use-expressions="true" pattern="/api/v1/**">
		<http-basic
			entry-point-ref="myBasicAuthenticationEntryPoint" />
	</http>

	<authentication-manager>
		<authentication-provider>
			<user-service>
				<user name="admin" password="{noop}admin" authorities="ROLE_USER" />
			</user-service>
		</authentication-provider>
	</authentication-manager>

	<global-method-security
		pre-post-annotations="enabled" />

	<beans:bean id="myBasicAuthenticationEntryPoint"
		class="io.crs.hsys.server.security.MyBasicAuthenticationEntryPoint" />

</beans:beans>